 * Refactor everything
 * Show merges as sub-trees
 * Show trees in different colors (branch colors)
 * Fix duplicate errors on big repos
 * Fix fatal errors on big repos
 * Show diffs when clicking on a commit
 * Show time of a commit
 * Show committer when clicking on a commit
 * Show hash and commit id when clicking on a commit
 * Show remote branches
 * Add search to commits
 * Allow refresh repos
 * Auto refresh repos?
 * Add a file chooser to choose a repo